import 'package:flutter/material.dart';

class AppColors {
  static const Color paleGreen = Color(0xFF43A047);       // Fresh eco green

  static const Color mint = Color(0xFF43A047);       // Fresh eco green

  static const Color peach = Color.fromARGB(255, 23, 160, 218);      // Warm bright orange accent
  static const Color blush = Color(0xFFE57373);      // Vibrant eco red
  static const Color cream = Color.fromARGB(255, 255, 139, 45);      // Light eco cream
  static const Color softWhite = Color(0xFFF5FFFA);  // Soft eco white
  static const Color deepTeal = Color(0xFF0288D1);   // Bright clean blue
  static const Color iceBlue = Color(0xFFF5F5F5);    // Neutral highlight background
}
